BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[SettingsAccount] ADD [bio] NVARCHAR(1000),
[email] NVARCHAR(1000),
[location] NVARCHAR(1000),
[phone] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
